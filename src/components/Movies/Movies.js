
import MoviesTable from './MoviesTable';



const Movies = () => {

    return (
        <>
            <MoviesTable />
        </>
    )
}

export default Movies;













{/*
const handlePagination = (movie) => {
        setPagination(movie)
    }
<div className="container d-flex flex-column">
                    <nav aria-label="...">
                        <ul class="pagination">

                            <li className={setPagination === "All movies" ? "page-item active" : "page-item"} onClick={() => handlePagination('All movies')} >All Movies</li>
                            <li className={setPagination === "Action" ? "page-item active" : "page-item"} onClick={() => handlePagination('Action')} >Action</li>
                            <li className={setPagination === "Comedy" ? "page-item active" : "page-item"} onClick={() => handlePagination('Comedy')}>Comedy</li>
                            <li className={setPagination === "Thriller" ? "page-item active" : "page-item"} onClick={() => handlePagination('Thriller')} >Thriller</li>

                        </ul>
                    </nav>
                </div> */}